name: main.yml
on:
  push:
    branches: [master]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm run deploy
        env:
          GIT_USER: redkubesbot
          PASSWORD: ${{ secrets.GITHUB_TOKEN }}
